import 'package:flutter/material.dart';

// Dark Muted Pink Theme
const primaryColor = Color(0xFF8C5459); // Deep muted rose (good for headers/buttons)
const secondaryColor = Color(0xFF1E0F13); // Medium muted pink for cards/panels
const bgColor = Color(0xFF2A1E1F); // Very dark muted pink/brown for background
// Sidebar specific color (dark muted burgundy/brown to match theme)
const sidebarColor = Color(0xFF1E0F13); // deep muted wine tone

const defaultPadding = 16.0;
